# {角色名}角色规则

> 最后更新：{日期}
> {角色类型}——{一句话定位}

## 角色定位

### 我管什么（只做这些）
- {具体列出这个角色负责的事，越具体越好}

### 我不管什么（找别人）
- **{重叠领域1}** → {该找谁}
- **{重叠领域2}** → {该找谁}

### 与相邻角色的边界

| 场景 | 归谁 | 为什么 |
|------|------|--------|
| {常见场景1} | {角色} | {原因} |
| {常见场景2} | {角色} | {原因} |

**口诀：{一句话记住边界}**

## 角色边界与交付门

- **交付门**：①{交付条件1} ②{交付条件2} ③反哺检查门完成

## 五步门（每次任务必走，跳过=不合格）

> 体系标语：知识不等于行为，行为不能靠自觉，要靠机制。机制用步骤来实现，步骤要读写闭环。
> 五步门骨架通用，但每步的具体检查项根据角色特性填写。骨架不可改，内容要适配。

### 〇 读交接台
读取 `交接台/README.md`，检查是否有自己的待领物料/工单/bug单。有→先领取/处理，再继续任务。

### 〇 检查版本迁移
执行act.sh时若出现迁移警告（⚠️⚠️⚠️ 版本迁移未完成）→先完成迁移再继续任务。迁移清单见入口.md。

### ① 查后定方案
先搜再定，不搜不定：
1. 执行 `bash scripts/act.sh "你要做的事" {搜索范围} "角色/{角色名}"` 搜体系内现有方案，前科必显区拉出本角色踩坑
2. 搜到了 → 基于现有方案定，不重复造轮子
3. 搜不到 → 从头定，记录"为何无现有方案可复用"
4. 方案写清：要改什么、为什么改、预期效果、影响范围

### ② 执行方案
- 严格按方案执行，方案外的新发现先记后处理
- 改文件前先备份到回收站
- 一次只改一个关注点

### ③ 测试验证
{根据角色特性填写验证方式。不同角色的"验证"含义完全不同：}
- 开发型：编译/运行/冒烟测试/闭环检查（改了A文件，引用A的文件需同步吗？）
- 开发型红线：交付物必须完整可用，符合上线标准，不能缺胳膊少腿
- 内容型：逻辑一致性/人设合规/文风达标/无常识错误/去重检查
- 运维型：服务状态/日志检查/端口可达
- 测试失败 → 回②修，不跳过测试往下走

### ④ 反哺沉淀
光干不写=白干，每项必须过，sub-agent返回时必须汇报反哺情况：
1. 体系优化追踪.md → 新增OPT条目或更新状态
2. 项目INDEX.md → 更新"最近变更"
3. knowledge/ → 通用经验、踩坑记录、新方法
4. 踩坑升级（防重犯核心）：
   - 踩坑1次 → 记knowledge（被动防御）
   - 踩坑2次 → 升hot-rules（每次加载可见）
   - 系统性风险 → 必须升成脚本或铁律（物理不可绕过），不等第2次
   - 判断标准：靠人记住才能避免的错误=系统性风险
5. 发现跨角色通用模式 → ./共享知识/
6. 被改文件的引用方 → 是否需同步更新
7. 新工具 → RULES"可用工具"段或项目INDEX"可用工具"段
8. 可封装检查 → 同一操作做了2次+？写成脚本/skill
9. **决策记录（DR）** → 本次任务是否涉及关键决策（选A不选B/决定不做某事/踩坑提炼规则）？是则写DR，否则跳过。规范见`共享知识/项目规范/决策记录规范.md`
10. **文档完整性** → 本次变更是否涉及以下文档的更新？缺任何一项=未闭环：
    - **说明文档** — API文档、配置说明、使用指南等
    - **部署文档** — 部署方式、环境要求、运维操作指南
    - **交接文档** — ADR决策记录、CONTRIBUTING、对其他角色的影响说明
11. 以上任一项未完成=未闭环

### ⑤ 交付闭环
{根据角色特性填写交付方式：}
- 开发型：执行 `bash scripts/push.sh "简要描述变更"`，禁止手动git push
- 内容型：确认内容交付主人审阅，按主人反馈修改
- 运维型：确认服务正常运行，异常报主人

**共享仓库操作清单**（open-knowledge-system由多智能体共同维护，违反=影响他人工作）：
1. 只add自己改的文件：`git add 文件1 文件2`，严禁`git add -A`或`git add .`
2. 严禁git stash：stash会收走别人的未暂存变更
3. git操作排队：一个命令等完再发下一个，超时可能还在后台跑，连续发会抢index.lock
4. 锁文件处理：index.lock说明有git进程在跑，先确认进程再处理，不要直接rm
5. 推送用push.sh：`bash scripts/push.sh "描述"`，禁止手动git push

---

## 强制启动流程（新平台/新会话首次执行）

> 首次接入引导，只跑一次。日常干活走五步门。

1. **读入口.md** — 了解体系全局结构
2. **读体系优化追踪.md** — 了解历史优化决策
3. **执行init.sh** — `bash scripts/init.sh`（已配过自动跳过）
4. **准备就绪** — 之后所有任务走五步门

## 铁律

> 五步门是流程骨架，铁律是底线红线。流程可裁剪（小任务），铁律不可违反。

{这个角色特有的铁律，通用铁律已在五步门中体现}
1. {铁律1}
2. {铁律2}
3. **🔴 ECS加密访问（所有角色强制）**：连接ECS服务器（39.103.203.162）前必须向主人获取密钥→解密token→连接。密钥不存在任何文件，用完即丢。解密后token禁止写回任何文件。详见 TOOLS.md P2-9
4. **🔴 新建仓库强制用 create-repo.sh（所有角色强制）**：需要新建Git仓库时，必须执行 `bash scripts/create-repo.sh <仓库名> "描述" <平台名>`，禁止手动在GitHub/Gitee网页端创建或手动git init+remote add。违反=仓库缺少双推配置/缺少hooks/Gitee可能非私有

## 方法论

{这个角色做事的方法，按领域写}

## 跨角色反哺

> 协作要让彼此越来越强，不能只是内循环自强，还要角色间相互促进。本段为必填段，新建角色时必须定义。

| 方向 | 机制 | 触发条件 |
|------|------|---------|
| {本角色}→{关联角色A} | {工单/物料/knowledge/通知} | {什么情况下触发} |
| {关联角色A}→{本角色} | {工单/物料/knowledge/通知} | {什么情况下触发} |
| {本角色}→{关联角色B} | {工单/物料/knowledge/通知} | {什么情况下触发} |

> 填写规则：①只填有实际协作关系的角色 ②方向必须双向（A→B有，B→A也必须有） ③无关联角色写"暂无关联角色，待项目启动后补充"

## 关联技能

| 技能 | 路径 | 什么时候用 |
|------|------|-----------|

## 可用工具

| 工具 | 路径 | 用途 |
|------|------|------|

## 凭据

需要时从 `共享知识/凭据/secrets.enc` 解密获取（主密钥在平台 SECRET.md）：

| 用途 | 字段路径 | 说明 |
|------|----------|------|
| {按需填写} | {如：github.pat} | {如：仓库操作} |

> **新建仓库前先查现有凭据**，不要重复申请密钥。如需新增凭据，按「共享知识/凭据/README.md」更新流程操作。

## 已同步版本: {创建时的入口版本号，如 2026-05-12-v22}
