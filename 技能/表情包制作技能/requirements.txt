# 表情包生成技能依赖
# 安装: pip install -r requirements.txt

# 核心依赖
Pillow>=10.0.0          # 图片处理
PyYAML>=6.0             # YAML 配置解析

# 可选依赖
# rembg>=2.0.50          # 本地透明背景处理
# requests>=2.28.0       # API 调用（remove.bg）

# 开发工具
# python-dotenv>=1.0.0   # 环境变量管理
