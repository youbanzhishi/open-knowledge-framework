# 混音母带与VST开发技能

## 技能说明

提供**音频混音/母带处理**和**VST插件开发**两大能力，支持贴唱混音、整体母带修复、VST插件开发全流程。

---

## 版本信息

- **版本**：3.1.0
- **创建日期**：2026-05-06
- **最后更新**：2026-05-13
- **变更日志**：
  - v3.1.0 新增VCMix里程碑M15~M18（音源分离与逆向分析/编曲智能模板/编曲混音一体化/完整开源DAW）；AI Agent差异化追加AI扒带/编曲分析/编曲混音一体化三项能力
  - v3.0.0 新增VCMix无界面混音宿主设计（YAML驱动+AI Agent友好+跨平台）、10个插件全部开发完成+CLI测试通过、DeEsser致命bug修复、Reverb wet信号滤波器、BPM同步Delay、surgical de-essing手工去齿音、混音自动化脚本vc_mix.sh、开源DAW路线图（M0~M14里程碑）
  - v2.0.0 贴唱混音流程链重构（先融合再清晰）、人声压缩优先+总线粘合底子、混响实现改用Schroeder算法、DSP/CLI/VST3三层分离架构、CI自动化测试规范、CLI使用指南规范、处理即测试+双保险理念、音色一致性>频率避让、清晰度vs融合度分频段分配、音量平衡四法、动态EQ衰减+静态EQ提升原则
  - v1.9.0 CI重构为插件独立Job并行构建+路径过滤+汇总打包发Release、新增本地编译验证铁律、新增VC-Smooth频谱共振抑制器、Actions日志三层降级获取策略、PAT认证配置
  - v1.8.0 Mac改用Ninja generator修复Xcode构建失败、统一artifact路径、沉淀GitHub Actions API调用方法
  - v1.7.0 修复GitHub Actions三平台构建失败，删除sed步骤，精简Linux依赖
  - v1.6.0 新增GitHub Actions CI编译流程、VC-Comp编译成功、更新踩坑记录
  - v1.5.0 新增VocalChain完整产品线设计（8个插件+1体链），产品设计文档作为唯一信源
  - v1.4.0 VC-EQ 编译成功，新增8条JUCE编译踩坑记录（CMAKE_C_COMPILE_OBJECT/IIR API变更/GTK禁用等）
  - v1.3.0 新增文档化与踩坑经验沉淀规则（强制执行）
  - v1.2.0 新增UI版本管理规则、审美自检流程
  - v1.1.0 新增UI自检流程（强制执行）
  - v1.0.0 初始版本

---

## 能力概览

| 能力 | 说明 | 状态 |
|------|------|------|
| 混音分析 | 频谱/响度/动态/声像/共振峰全维度分析 | ✅ 成熟 |
| 贴唱混音 | 人声+伴奏两轨混音处理 | ✅ 成熟 |
| 整体母带修复 | 无分轨情况下的整体EQ/限幅/响度修复 | ✅ 成熟 |
| VST插件开发 | JUCE框架开发VST3插件 | 🔄 原型阶段 |
| VCMix混音宿主 | YAML驱动无界面混音宿主，AI Agent友好 | 🔄 开发中 |

### 产品线设计（唯一信源：`知识沉淀/VocalChain插件系列产品设计文档.md`）

| 插件 | 对标 | 状态 |
|------|------|------|
| VC-EQ | FabFilter Pro-Q / Waves Q10 | 🟢 已编译 |
| VC-Comp | Waves RComp | 🟢 已编译 |
| VC-Smooth | Oeksound Soothe2 / Baby Audio Reso | 🔄 开发中 |
| VC-DeEsser | Waves DeEsser / FabFilter Pro-DS | 🟢 已编译（bug已修复） |
| VC-Reverb | Waves RVerb / FabFilter Pro-R | 🟢 已编译（+wet滤波器） |
| VC-Delay | Waves H-Delay / FabFilter Timeless | 🟢 已编译（+BPM同步） |
| VC-Limiter | Waves L2 / FabFilter Pro-L | 🟢 已编译 |
| VC-Saturator | — | 🟢 已编译 |
| VC-Gain | — | 🟢 已编译 |
| VC-DynamicEQ | — | 🟢 已编译 |
| VC-Distortion | Waves Berzerk / FabFilter Saturn | 📋 待开发 |
| VC-Noise | MDA Test Tone / Signalizer | 📋 待开发 |
| VC-SurgicalDeEsser | 手工精准去齿音 | 📋 待开发 |
| VCMix | YAML驱动无界面混音宿主 | 🔄 开发中 |
| VocalChain 一体链 | — | 📋 待开发 |

> ⚠️ 所有插件的功能规格、参数范围、UI设计决策都记录在产品设计文档中，新会话启动时读取该文档即可无缝衔接。

---

## 一、混音分析

### 触发条件
- 用户提供音频文件，要求分析混音问题
- 混音/母带处理前必做分析

### 分析维度与工具

| 维度 | 工具 | 产出 |
|------|------|------|
| 频谱能量分布 | librosa + scipy | 频谱图、频段能量分布图 |
| 响度测量 | ffmpeg ebur128 | LUFS、LRA、True Peak |
| 动态范围 | numpy RMS/峰值计算 | RMS包络、Crest Factor、LRA直方图 |
| 立体声声像 | librosa + numpy | Mid/Side能量对比、Lissajous图、立体声宽度 |
| 时变频谱 | librosa Mel频谱 | 频谱热力图 |
| 共振峰 | scipy 频率响应分析 | 频率响应曲线+共振峰标注 |

### 问题诊断分级

🔴 **必须修复**：
- True Peak 超过 0dBFS → 限幅至 -1 dBTP
- 削波失真 → 降低增益

🟠 **重点改进**：
- 高频缺失（2-12kHz能量不足）→ EQ提升
- Air感不足（12-20kHz接近无声）→ 高频搁架提升
- 响度偏低（LUFS < -16）→ 增益/压缩调整
- 低频浑浊（100-300Hz堆积）→ 低频衰减/低切
- 动态异常（LRA >10 或 <3）→ 压缩调整

✅ **良好指标**：
- 立体声平衡：左右差 <0.5dB
- Crest Factor：10-15dB
- 低频单声道兼容性：相关性 >0.85

### 流行音乐参考标准

| 指标 | 目标值 |
|------|--------|
| Integrated LUFS | -14 ~ -10 |
| True Peak | ≤ -1 dBTP |
| LRA | 4 ~ 8 dB |
| Crest Factor | 10-15 dB |

### 执行流程
```
1. 读取音频文件
2. ffprobe 获取基本信息（采样率、位深、声道、时长）
3. ffmpeg ebur128 测量 LUFS/LRA/True Peak
4. Python 生成各维度分析图表
5. 汇总问题诊断，生成 Markdown 报告
6. 所有产出保存到 ./混音分析/ 目录
```

---

## 二、贴唱混音

### 触发条件
- 用户提供人声干声 + 伴奏文件
- 用户说"帮我混音"、"做贴唱混音"等

### 输入要求
- **人声**：WAV格式，纯干声（不加任何效果）
- **伴奏**：WAV/FLAC格式
- **可选**：参考曲（风格相近的成品曲，用于对标）

### 人声处理链

> ⚠️ **v2.0核心原则：先融合再清晰，处理层数越少越好**
> 
> 融合是地基，清晰是装修。地基没打好就装修，一动全白费。
> 处理层数越少越好——每层都吃动态，四层轻处理叠加=一层重处理。

#### 推荐处理链（v6验证版）

```
人声干声
  → [1] 人声压缩（2.5:1, 统一动态，主歌副歌音量均衡）
  → [2] [可选] 动态EQ微修（只压2-3个最冒的共振峰，最大2-3dB）
  → [3] [可选] 轻微混响（Plate, wet 10-12%, 高频衰减保清晰）
  → [4] [可选] De-essing（齿音明显时加）
  ↓
音量平衡（人声比伴奏高1-2dB）
  ↓
混合 → 总线粘合压缩（1.2:1, 0.5-1dB衰减）
  ↓
响度标准化（-14 LUFS, -1.5 dBTP）
```

#### 处理优先级（必须按顺序）
1. **必须**：人声压缩 + 音量平衡 + 总线粘合 → 这是融合的底子
2. **建议**：轻微混响（带高频衰减）→ 共享空间=融合+清晰兼得
3. **按需**：动态EQ微修 → 只修最冒的点，不是全面处理
4. **按需**：De-essing → 有齿音才加

#### 1. 人声压缩（最关键！）

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| Ratio | 2.5:1 | 统一动态，主歌副歌音量差变小 |
| Threshold | 人声RMS下方3-4dB | 让主歌也进入压缩 |
| Attack | 10ms | 快速响应峰值 |
| Release | 100ms | 慢速释放，避免抽吸 |
| Makeup Gain | 补偿增益衰减量 | 保持整体响度 |
| 目标增益衰减 | 3-5dB | 不要压太狠 |

> 压缩的核心价值：让声音从"时有时无的信号"变成"持续饱满的存在"。
> 没压缩的主歌人声小、副歌大，一个增益值照顾不了两端。

#### 2. EQ原则

> ⚠️ **衰减用动态EQ，提升用静态EQ**
> - 动态EQ：只在超标时压，安静时不动，保持自然
> - 静态EQ：持续提升做调色，给人声加色彩
> - 动态EQ替代不了静态EQ做调色

| 频段 | 频率 | 类型 | 增益 | Q值 | 用途 |
|------|------|------|------|-----|------|
| 低切 | 80Hz | 高通 | - | 6dB/oct | 去闷声和低频噪音 |
| 箱体共振 | 250Hz | 钟形 | -1.5~-2dB | 1.5 | 减少浑浊感 |
| 人声存在感1 | 3.5kHz | 钟形 | +2~+4dB | 1.2 | 歌词清晰度 |
| 人声存在感2 | 4.5kHz | 钟形 | +2~+3dB | 1.5 | 补充存在感 |
| 亮度 | 8kHz | 钟形 | +2~+2.5dB | 1.2 | 增加亮度 |
| 空气感 | 12kHz | 高频搁架 | +1.5~+2dB | - | 增加空间感 |

⚠️ EQ提升要保守，每个处理步骤后检查峰值。**色彩EQ要"补"不要"抢"**——对着伴奏的频谱空位做。

#### 3. 动态EQ微修（可选，解决"偶尔冒"）

只压2-3个最突出的共振峰，每个最多衰减2-3dB，只在超标时压。

示例：
| 频率 | Q值 | 阈值 | 最大衰减 |
|------|-----|------|----------|
| 3400Hz | 2.0 | -6dB | 2.5dB |
| 6000Hz | 2.5 | -8dB | 2dB |

#### 4. 混响（可选，补空间感+融合）

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| 风格 | Plate（板式） | 适合流行人声 |
| 干湿比 | wet 10-12% | 极轻，现代趋势：干声为主 |
| 衰减时间 | 1.5~2秒 | 中国风/抒情可稍长 |
| Pre-delay | 20~30ms | 让干声先出来，避免糊 |
| 高频衰减 | 6kHz以上渐衰 | **关键！** 低中频混响给融合，高频衰减保清晰 |

> ⚠️ 混响实现必须用Schroeder算法（4梳状+2全通），禁止用延迟线+随机噪声模拟！
> 高频衰减必须加在混响湿信号上，不是IR上。

#### 5. 去齿音（可选）

- 检测频段：5-8kHz
- 衰减不超过3dB
- 只在明显时才加

### 伴奏处理

- 伴奏已是成品，尽量少动
- 2-4kHz 轻微衰减 -1~-1.5dB，给人声腾频段空间（侧链融合）

### ⚠️ 音量平衡（最关键）

**核心痛点**：静态音量平衡解决不了动态跨度大的问题——副歌人声大、主歌人声小。

#### 贴唱音量平衡四法

1. **LUFS对齐法（最客观）**：用响度表看数字，人声LUFS比伴奏高1-2dB。REAPER挂JS: Loudness Meter
2. **聊天音量法（最直觉）**：调到日常聊天音量，人声每个字清楚但不吵，伴奏垫着不抢
3. **闭眼测试**：人声在面前、伴奏在后面=对了；人声怼脸上=大了；听不清词=小了
4. **手机喇叭测试**：手机外放频段窄，平衡不对特别明显。手机上听着舒服八成就对了

#### 动态不均匀的解法（主歌副歌音量差大）
- **人声压缩**（首选）：ratio 2.5:1, attack 10ms, release 100ms, 增益衰减3-5dB
- **音量自动化**：手动画包络，主歌推高副歌不动
- **最稳**：压缩+自动化结合，压缩管大范围，自动化管小范围

### 总线处理（增加融合度）

| 参数 | 值 | 说明 |
|------|-----|------|
| Ratio | 1.2:1 | 极轻粘合，不是压平 |
| Threshold | 混音RMS以下约6dB | — |
| Attack | 50ms | 中速响应 |
| Release | 200ms | 慢速释放 |
| 目标增益衰减 | 0.5-1dB | 超过1dB就太多了 |

> 总线粘合的核心：让人声和伴奏"一起呼吸"，不是压扁。

### 母带处理

| 步骤 | 目标 |
|------|------|
| True Peak 限幅 | ≤ -1 dBTP |
| 响度标准化 | -14 LUFS |
| 输出格式 | WAV 16bit 44.1kHz + MP3 320kbps |

### 执行流程
```
1. 分析干声和伴奏的频谱/响度/动态
2. 确认采样率/时长对齐
3. 人声压缩（统一动态，最关键！）
4. [可选] 动态EQ微修（只修最冒的点）
5. [可选] 轻微混响（带高频衰减，补空间感）
6. [可选] 去齿音（有齿音才加）
7. 音量平衡（人声比伴奏高1-2dB）
8. 混合 → 总线粘合压缩（极轻，0.5-1dB）
9. 母带处理：True Peak ≤ -1.5dBTP, 响度 -14 LUFS
10. 验证：测量最终LUFS/True Peak/LRA
11. 生成前后对比图和报告
```

> ⚠️ **先做融合（压缩+平衡+总线粘合），再修细节（动态EQ+混响）**
> 在融合状态下听，更容易发现真正的问题。独听修EQ，混进去大概率修过头。

### 输出规范
- 所有产出保存到 `./混音分析/` 目录
- 文件命名：`{歌名}-混音成品.wav`、`{歌名}-混音成品.mp3`、`{歌名}-人声处理后.wav`
- **先出MP3试听，用户满意后再出WAV**（省时间）
- 图表：频谱对比图、响度对比图、电平对比图
- 报告：`{歌名}-混音处理报告.md`，记录所有参数和REAPER复现步骤

### ⚠️ 混音处理报告规范（强制执行）
**报告是用户学习和复现的唯一依据，必须详细到能当教程看！**

每份报告必须包含以下内容：

1. **频谱共振检测结果**（精确诊断）：
   - 哪些频率在冒（精确到Hz）
   - 什么时候冒（时间点或时段）
   - 冒了多少dB
   - 用表格列出，示例：2.3kHz在01:12-01:18冒了+6dB，4.1kHz在02:30-02:35冒了+4.5dB

2. **每个处理环节的详细参数**：
   - 频谱共振抑制：阈值(dB)、深度、作用频段
   - 分频段压缩：每个频段的threshold/ratio/attack/release/增益量
   - EQ：每个频段的频率/Q值/增益/类型
   - 混响：类型/pre-delay/decay/wet比/高频衰减
   - 侧链融合：频段/Q值/衰减量/attack/release
   - 总线粘合：threshold/ratio/attack/release/实际压缩量
   - 限幅：threshold/实际增益减少量
   - 电平：人声vs伴奏的RMS差值

3. **每个问题的"诊断→处理→效果"三段式**：
   ```
   问题：2.3kHz共振峰在副歌部分冒出+6dB导致刺耳
   处理：动态频谱抑制，阈值4dB，深度0.6，频段1k-10kHz
   效果：该频率在冒时被衰减3.6dB，安静时不受影响
   ```

4. **REAPER复现指南**：
   - 每个处理步骤对应REAPER里该挂什么插件
   - 具体参数怎么设
   - 信号路由怎么接（特别是侧链）

**目的**：用户拿着报告就能在REAPER里一步步复现，同时理解为什么要这么处理。

---

## 贴唱混音核心认知（v2.0新增）

### 融合的本质：共同点

**融合 = 共同点。人声和伴奏共享的东西越多，听感越一体。**

融合的四个层次（按重要性排序）：

1. **共享空间**（最有效）：同一间房里的两个声音天然融合。混响/房间模拟=把人声和伴奏放进同一个"房间"
2. **共享总线处理**（最直接）：总线粘合压缩=一起被压一起释放=动态节奏同步="一起呼吸"。总线EQ=统一色调。总线饱和=加同样谐波=音色趋同
3. **共享音色**（音色一致性）：同暖同亮=一家人；一暖一亮=分离感
4. **频率避让**（最底层）：解决"能听清"，不解决"融得好"。不打架≠融合

**让声音"在一起"，比让声音"不打架"重要得多。**

### 清晰度 vs 融合度的矛盾与解法

- 越清晰→声音越分离；越融合→声音越靠拢
- 不是均匀折中，是**分频段分配**：
  - 低频：让给融合（人耳定位差，粘在一起才稳）
  - 中频：精打细算的主战场
  - 高频：让给清晰（空气感、齿音细节让人声"浮出来"）
- 混响高频衰减是核心解法：低中频混响给融合，高频衰减保清晰

### 处理层数越少越好

每层处理都吃动态。四层轻处理叠加=一层重处理的效果。
九万字实战对比：极简版（2层）> v6（3层）> v5（4层压扁）> v4（6层没感情）

### 混响实现铁律

- ❌ 禁止用延迟线+随机噪声模拟混响（产生沙沙声、金属感）
- ❌ 禁止把高频衰减加在IR上
- ✅ 必须用Schroeder算法（4梳状+2全通）或sox reverb
- ✅ 高频衰减加在混响湿信号（wet_signal）上

---

## 三、整体母带修复

### 触发条件
- 用户只有最终混音文件，没有分轨
- 用户说"修一下母带"、"修修频谱"等

### 适用场景
- 无分轨，只能做整体处理
- EQ调整必须保守

### EQ修复参考参数

| 频段 | 频率 | 增益 | 说明 |
|------|------|------|------|
| 人声存在感 | 3000Hz | +3~+4dB | 钟形 |
| 清晰度 | 6500Hz | +2~+3dB | 钟形 |
| 亮度 | 10000Hz | +3~+4dB | 钟形 |
| Air感 | 14000Hz | +2~+3.5dB | 高频搁架 |

⚠️ 整体处理EQ要保守，宁可欠不要过，避免某些频段刺耳。

### 动态处理
- 温和总体压缩（ratio 1.5:1-2:1）
- 目标 LRA 6-8dB

### 母带标准
- True Peak ≤ -1 dBTP
- 响度 -14 LUFS
- 使用 ffmpeg loudnorm 滤镜实现

---

## 四、VST插件开发

### 触发条件
- 用户要求开发VST/AU音频插件
- 用户说"做个插件"、"开发VST"等

### 技术选型：JUCE 框架（C++）

| 优势 | 说明 |
|------|------|
| 多格式输出 | VST3 / AU / AAX / Standalone 一次开发 |
| 内置DSP模块 | 滤波器、压缩、延迟、混响算法 |
| 自带UI框架 | 旋钮、推子、频谱显示组件 |
| 社区成熟 | 教程和示例代码丰富 |

### UI方案选型

| 方案 | 性能 | 视觉效果 | 开发难度 | 推荐 |
|------|------|---------|---------|------|
| 预渲染图片 | ⭐最低 | 好 | 中 | ⭐首选 |
| JUCE自绘 | 低 | 取决于投入 | 高 | 定制化需求 |
| Web UI | 🔴高 | 最好 | 高 | 需要前端资源 |
| OpenGL | 低 | 炫酷 | 最高 | 动画特效需求 |

**推荐方案：预渲染图片 + 轻量DSP**
- 旋钮旋转=换PNG帧，不需要实时计算绘制
- 静态元素只加载一次
- 不需要每帧重绘，只在参数变化时刷新
- 性能最省，视觉也不差

### JUCE线程架构
```
UI线程 ←→ 参数桥（原子操作）←→ DSP线程（实时音频）
```
- UI和DSP自动分线程，互不干扰
- UI卡顿不影响声音，DSP吃满不卡界面

### 开发流程
```
1. 从VC-Plugin-Template模板创建项目
2. 编写DSP核心算法（Source/DSP/）
3. 编写VST3壳（Source/PluginProcessor）调用DSP
4. 编写CLI壳（Source/CLI/）调用同一DSP
5. 编写UI（Source/PluginEditor）
6. 编写测试（tests/verify_{name}.py + 测试音频）
7. ⚠️ 本地编译验证（铁律：推GitHub前必须本地编译通过）
8. 本地运行CLI测试通过
9. 推送到GitHub → CI自动build+test → 三平台全绿才发版
```

### DSP/CLI/VST3三层分离架构（v2.0强制规范）

**所有VocalChain插件必须遵守此架构：**

```
VC-{PluginName}/
├── Source/
│   ├── DSP/                ← 核心算法（插件和命令行共用）
│   │   ├── {PluginName}DSP.h
│   │   └── {PluginName}DSP.cpp
│   ├── PluginProcessor.h   ← VST3插件壳（调用DSP/）
│   ├── PluginProcessor.cpp
│   ├── PluginEditor.h      ← VST3 UI壳
│   ├── PluginEditor.cpp
│   └── CLI/                ← 命令行工具（调用DSP/）
│       └── main.cpp
├── tests/
│   ├── test_audio/         ← 测试用音频
│   ├── golden/             ← golden reference
│   ├── verify_{name}.py    ← 验证脚本
│   └── run_tests.sh
├── CMakeLists.txt          ← 同时构建VST3 + CLI两个target
└── README.md
```

**架构优势**：算法只写一遍，VST3和CLI完全复用。CLI用于云电脑批量处理和CI测试，VST3用于本地REAPER实时使用。

### CI自动化测试规范（v2.0强制规范）

**编译通过≠插件正确，测试通过才算真通过。**

所有VocalChain插件CLI版本必须纳入CI自动化测试：

- **功能测试**：已知输入→预期输出，验证算法正确性
- **参数边界测试**：参数=0→输出=输入（bypass）；参数=最大值→不崩溃
- **跨平台一致性测试**：三平台输出差异在float精度内
- **回归测试**：存golden reference，每次对比

CI流程：detect → build → test → release（测试通过才发版）

### 双保险理念

- CI测试管"有没有bug"（功能正确性、跨平台一致性、回归）
- 真实混音管"好不好听"（主观听感、真实场景表现）
- 每次用CLI做混音 = 一次真实场景集成测试
- 两条腿走路，缺一不可

### ⚠️ 本地编译验证铁律
**推送到GitHub前，必须在云电脑上本地编译验证通过！** 避免低级错误浪费CI时间。
- 编译命令：`cd {插件}/build && cmake .. -DJUCE_PATH=/opt/JUCE && cmake --build . --config Release`
- 本地通过后再 `git push`
- 本地编译失败 = 禁止推送

### 可开发的插件类型

| 类型 | 复杂度 | 说明 |
|------|--------|------|
| EQ插件 | ⭐⭐ | 参数均衡器+频谱显示 |
| 压缩器 | ⭐⭐ | 带增益衰减表 |
| 分析器 | ⭐⭐ | 频谱/响度/声像表 |
| 混响 | ⭐⭐⭐ | 卷积或算法混响 |
| 多效果链 | ⭐⭐⭐ | 一体化人声处理 |

### 产品策略：独立插件系列 + 一体链

```
VocalChain 系列（统一设计语言、深色主题）
├── VC-EQ      — 5段参数均衡器 + 频谱显示（400x300px）
├── VC-Comp    — 压缩器 + GR增益衰减表（400x300px）
├── VC-DeEsser — 去齿音：频率+阈值+衰减量（400x300px）
├── VC-Reverb  — 混响（400x300px，天然支持Send模式）
├── VC-Delay   — 延迟（400x300px，天然支持Send模式）
└── VocalChain — 一体链快捷入口（800x500px，Insert模式预设）
```

**为什么做独立插件系列**：
- 灵活性：任意顺序/组合，Send/Insert由DAW路由控制
- CPU效率：只挂需要的
- 深度：每个插件可以加专业功能（频谱/GR表/A-B对比）
- 一体链作为"快捷入口"存在，给不想折腾的用户用

### 独立插件：VC-EQ（参数均衡器）

**5-7段全参数化**：
| 参数 | 说明 |
|------|------|
| 频率 | 每段可调 |
| Q值 | 每段可调 |
| 增益 | 每段可调 |
| 频段类型 | 低切/低搁架/钟形/高搁架/高切切换 |
| 频谱显示 | 实时频谱 + EQ曲线叠加 |

### 独立插件：VC-Comp（压缩器）

- Threshold / Ratio / Attack / Release / Makeup
- GR增益衰减实时表
- Auto Makeup Gain

### 独立插件：VC-DeEsser（去齿音）

- 频率选择（3-10kHz可调）
- 阈值控制
- 衰减量控制

### 独立插件：VC-Smooth（频谱共振抑制器）

**推荐处理顺序**：Smooth → EQ → Comp（先去刺耳，再调色，再控动态）

| 参数 | 范围 | 默认值 | 说明 |
|-----|------|--------|------|
| Depth | 0-100% | 0% | 核心旋钮，共振衰减深度 |
| Speed | Slow/Med/Fast | Med | 衰减响应速度（attack/release联动） |
| Freq Low | 20-20000Hz | 20Hz | 处理频段下限（默认全频段） |
| Freq High | 20-20000Hz | 20000Hz | 处理频段上限（默认全频段） |
| Sharpness | Soft/Normal/Sharp | Normal | 检测灵敏度，Sharp抓更细的峰 |
| Mix | 0-100% | 100% | 干湿比，方便parallel处理 |

**技术方案**：FFT分帧 → 频谱峰值检测 → 动态增益计算 → 频域衰减 → IFFT还原
**产品设计文档**：`知识沉淀/插件开发/VC-Smooth产品设计文档.md`

### 独立插件：VC-Reverb（混响）

- 类型：板式/大厅/房间
- Wet/Decay/Pre-delay
- Send模式下干声保持干净

### 独立插件：VC-Delay（延迟）

- Time/Feedback/Mix
- 同步BPM选项
- Send模式支持

### 一体链：VocalChain（快捷入口）

**预设方案**：
| 预设名 | EQ侧重 | 压缩 | 混响 | 适用 |
|--------|--------|------|------|------|
| 流行贴唱 | 存在感+3dB | 2:1温和 | 板式85:15 | 通用流行 |
| 中国风 | 亮度+2dB | 2:1 | 板式80:20 | 古风/国风 |
| 温暖人声 | 中低频+1dB | 2.5:1 | 大厅82:18 | 民谣/抒情 |
| 通透人声 | 高频+3dB | 1.5:1 | 板式90:10 | 硬核/说唱 |

**通用功能**：
- Input/Output增益
- A/B对比
- 整体Bypass

---

## 技术栈汇总

| 类别 | 工具 |
|------|------|
| 音频分析 | Python + librosa, scipy, numpy |
| 音频处理（主力） | VC插件CLI（vc-smooth/vc-eq/vc-comp等） |
| 音频处理（辅助） | sox（基础操作）, scipy.signal, numpy |
| 响度测量 | ffmpeg ebur128 / loudnorm |
| 混响实现 | Schroeder算法（4梳状+2全通），禁止延迟线+噪声模拟 |
| 压缩实现 | 自写压缩器逻辑 / vc-comp CLI |
| EQ实现 | scipy.signal.iirfilter / vc-eq CLI |
| VST开发 | JUCE 8 + CMake + C++ |
| CI测试 | GitHub Actions + CLI + sox生成测试音频 + Python验证 |

---

## 输出目录规范

```
./混音分析/
├── {歌名}-混音成品.wav        # 最终混音WAV
├── {歌名}-混音成品.mp3        # MP3试听版
├── {歌名}-人声处理后.wav      # 单独人声轨
├── {歌名}-混音处理报告.md     # 参数+REAPER复现步骤
├── {歌名}-母带修复.wav        # 母带修复版（如有）
├── {歌名}-母带修复.mp3
├── {歌名}-母带修复报告.md
├── 频谱图.png                 # 分析图表
├── 响度分析.png
├── 动态范围.png
├── 声像分析.png
├── 时变频谱.png
├── 频段能量分布.png
├── 共振峰分析.png
├── 混音前后频谱对比.png
├── 混音前后响度对比.png
├── 人声vs伴奏电平对比.png
├── 修复前后频谱对比.png
├── 修复前后响度对比.png
└── analysis_data.json         # 原始分析数据
```

---

## 踩坑记录

### v1→v2 教训：电平平衡是第一优先级
- 问题：人声被伴奏盖住，只能听到混响
- 原因：电平差只有+1.4dB，混响80:20太湿
- 修正：电平差调到+7.5dB，混响改90:10
- **结论**：电平平衡 > 一切效果处理

### v2→v3 教训：过犹不及
- 问题：人声失真 + 融合度差
- 原因：电平差7.5dB太大导致失真，混响90:10太干导致人声飘在上面
- 修正：电平差3-4dB甜区，混响85:15，加总线粘合压缩
- **结论**：
  - 电平差3-4dB是贴唱甜区
  - 混响不能太干也不能太湿
  - 总线粘合压缩是人声融入伴奏的关键
  - EQ增益要保守，每步检查峰值

### 整体母带修复教训
- 无分轨时EQ必须保守，宁可欠不要过
- 高频提升幅度3-5dB是安全范围
- 使用ffmpeg loudnorm做响度标准化比手动增益调整更准确

### v2.0 贴唱混音实战教训（九万字迭代）

| 版本 | 处理 | 结果 | 教训 |
|------|------|------|------|
| v4 | 六层重处理 | 完全没感情 | 过处理杀动态 |
| v5 | 四层轻处理 | 波形压扁，难听 | 叠加效应，每层都吃动态 |
| 极简版 | 音量平衡+总线微压 | 融合OK但偶尔冒、动态不均 | 融合的基础 |
| v6 | +人声压缩2.5:1 | **动听！** | 压缩是关键，先融合再细节 |
| v7原版 | +动态EQ+混响(假) | 混响出问题 | 混响必须用Schroeder算法 |
| v7-fix | +动态EQ+Schroeder混响 | 待验证 | 混响实现铁律 |

**核心教训**：
- 先融合再清晰，融合是地基
- 处理层数越少越好
- 混响禁止用延迟线+噪声模拟
- 独听修EQ，混进去大概率修过头

---

## ⚠️ UI自检流程（强制执行）

每次生成UI设计稿后，**必须**执行以下自检清单，全部通过才能交付给用户。未通过的项目必须修正后重新生成。

### 布局自检

| 检查项 | 标准 | 不合格示例 |
|--------|------|-----------|
| **控件间距** | 同类控件之间至少20px间距，不同类控件之间至少30px | 旋钮挤在一起，无法区分 |
| **输入左/输出右** | Input/Gain相关控件放左侧，Output相关控件放右侧 | 输入输出旋钮放在中间挤着 |
| **呼吸空间** | 各区域之间有明显分隔（分隔线或间距），不能满屏堆砌 | 底部旋钮区密密麻麻 |
| **对齐** | 同行控件垂直居中对齐，同列控件左对齐 | 旋钮高低不一 |
| **Padding** | 窗口边缘至少15px内边距，控件不贴边 | 按钮紧贴窗口边缘 |
| **分区清晰** | 功能区域用背景色差异或分隔线区分 | 所有控件混在一起 |

### 交互自检

| 检查项 | 标准 |
|--------|------|
| **Bypass可见** | 旁通按钮必须醒目，一眼就能找到 |
| **A/B可达** | A/B切换按钮在标题栏或显眼位置 |
| **频段选择可达** | 频段切换按钮排列整齐，点击区域足够大 |
| **扫频按钮可达** | Sweep/Solo按钮紧挨对应频段控制区 |
| **预设管理可达** | 预设下拉框在底部或顶部固定位置 |

### 专业性自检（对标Waves/FabFilter）

| 检查项 | 标准 |
|--------|------|
| **输入左/输出右** | 遵循行业通用布局（Waves/FabFilter标准） |
| **电平表跟对应旋钮同侧** | 输入电平表靠左，输出电平表靠右 |
| **GR表居中** | 增益衰减表在输入输出之间 |
| **频谱占主面积** | EQ类插件的频谱显示应占界面50%以上面积 |
| **旋钮大小统一** | 同类旋钮直径一致（通常50-60px） |
| **色彩层次** | 标题栏>频谱区>控制区>底部，由深到浅 |
| **标签清晰** | 每个控件下方/旁边有文字标注 |

### 自检执行方式

```
生成UI设计稿
  ↓
逐一检查上述清单（逐项打勾）
  ↓
发现不合格项 → 修正布局参数 → 重新生成
  ↓
全部通过 → 交付用户
```

### 审美与用户体验自检（强制执行）

| 检查项 | 标准 | 不合格示例 |
|--------|------|-----------|
| **视线连贯** | 用户最常看的信息放在同一视线区域，不需要上下/左右频繁切换视线 | 频谱在中间，电平表在最底部，看频谱和看电平要反复切换视线 |
| **关联信息就近** | 有关联的控件必须紧挨着 | 电平表和增益旋钮离得很远 |
| **留白呼吸** | 界面不能填满，必须有足够的留白区域 | 控件铺满整个窗口，没有视觉喘息空间 |
| **视觉重心** | 界面要有明确的视觉重心（如频谱区），其他元素围绕它分布 | 所有区域同等大小，没有重点 |
| **参照顶级插件** | 布局前先看 FabFilter/Waves/SSL 等顶级插件的同类产品是怎么排的 | 凭自己想象排列控件 |
| **换位思考** | 假设自己是混音师，打开插件的5秒内能否理解布局？常用操作是否顺手？ | 找个Bypass按钮要扫视整个界面 |

### 参考布局（专业EQ插件标准）

**FabFilter Pro-Q 风格**：
- 频谱占主区域
- 输入电平表在频谱左侧竖排
- 输出电平表在频谱右侧竖排
- GR表在频谱右边缘
- 参数控制在底部
- Bypass/A/B在顶部标题栏

**Waves Q10 风格**：
- 频谱居中
- 两侧竖向电平表
- 频段参数在频谱下方
- 顶部预设和Bypass

### UI版本管理规则（强制执行）

每次UI设计迭代必须遵循以下规则：

1. **版本目录命名**：每个版本保存到独立目录 `VocalChain-UI/v{版本号}/`
   ```
   VocalChain-UI/
   ├── v1/  ← 第一版（初始设计）
   ├── v2/  ← 第二版（修正旋钮拥挤）
   ├── v3/  ← 第三版（电平表移频谱两侧）
   ├── v4/  ← 第四版（参照FabFilter Pro-Q重做）
   └── latest/  ← 软链接指向当前最新版本
   ```

2. **版本记录**：每次迭代必须记录在 `VocalChain-UI/版本记录.md` 中
   ```markdown
   | 版本 | 日期 | 修改内容 | 用户反馈 | 状态 |
   |------|------|---------|---------|------|
   | v1 | 2026-05-06 | 初始设计 | 旋钮拥挤、输入输出位置不对 | ❌ 废弃 |
   | v2 | 2026-05-06 | 修正旋钮间距、输入左输出右 | 电平表和频谱分离、预设在底部 | ❌ 废弃 |
   ```

3. **不覆盖旧版**：严禁直接覆盖上一版图片，必须创建新版本目录

4. **质量自检后再交付**：生成图片后先视觉检查（控件是否重叠、间距是否合理、布局是否参照顶级插件），通过后再交付用户

### UI版本记录

| 版本 | 日期 | 修改内容 | 用户反馈 | 状态 |
|------|------|---------|---------|------|
| v1 | 2026-05-06 10:18 | 初始VocalChain一体链设计 | 风格OK，EQ太笼统 | ❌ 需重构 |
| v2 | 2026-05-06 10:51 | VC-EQ独立插件初始设计 | 旋钮拥挤、输入输出位置不对 | ❌ 废弃 |
| v3 | 2026-05-06 10:55 | 修正旋钮间距、输入左输出右 | 电平表应放频谱两侧、预应在顶部 | ❌ 废弃 |
| v4 | 2026-05-06 11:11 | 电平表移频谱两侧、预设移顶部 | 布局有问题、按钮重叠、审美差 | ❌ 废弃 |
| v5 | 2026-05-06 | 参照FabFilter Pro-Q实际截图重做 | 待确认 | 🔄 进行中 |

### 已知踩坑记录

| 日期 | 问题 | 教训 |
|------|------|------|
| 2026-05-06 | VC-EQ底部旋钮拥挤，输入输出旋钮没按左右分布 | 控件间距必须≥20px，输入左输出右是行业铁律 |
| 2026-05-06 | VC-EQ电平表和GR表放在底部，和频谱分离 | 电平表应放频谱两侧竖排，视线连贯，参照FabFilter Pro-Q |
| 2026-05-06 | 整体审美差，功能堆砌 | 必须先看顶级插件的布局再设计，不能凭想象排控件 |
| 2026-05-06 | v4版按钮重叠、布局混乱 | 生成后必须先视觉自检再交付，控件不能重叠 |
| 2026-05-06 | 每次改版覆盖旧文件，无法回退 | 必须版本目录隔离，严禁覆盖旧版 |
| 2026-05-06 | 反复修改审美不达标 | 必须先搜索顶级插件截图作为参考，照着布局比例做 |
| 2026-05-06 | **CMake配置juceaide报CMAKE_C_COMPILE_OBJECT错误** | project()必须声明`LANGUAGES C CXX`，否则juceaide子进程编译C文件失败 |
| 2026-05-06 | **CMake找不到JUCE模块** | 必须使用`add_subdirectory(${JUCE_PATH} JUCE-build)`而非`include(JUCE)` |
| 2026-05-06 | **JuceHeader.h不存在** | 不使用Projucer时，直接include模块头文件如`<juce_audio_processors/juce_audio_processors.h>` |
| 2026-05-06 | **IIR::Coefficients API差异** | JUCE 8中`makePeakEQ`改为`makePeakFilter`，系数通过`filter.coefficients`设置而非`filter.state` |
| 2026-05-06 | **GTK/WebKit缺失导致编译中断** | 禁用GTK：`target_compile_definitions(VC-EQ PRIVATE JUCE_USE_GTK=0 JUCE_WEB_BROWSER=0)` |
| 2026-05-06 | **libcurl未链接** | 禁用curl：`JUCE_USE_CURL=0`，VST插件通常不需要网络功能 |
| 2026-05-06 | **VST2/VST3自动化冲突** | 禁用VST2：`JUCE_VST3_CAN_REPLACE_VST2=0` |
| 2026-05-06 | **createEditor()未定义** | PluginProcessor.cpp中必须实现`createEditor()`返回编辑器实例 |
| 2026-05-06 | **VC-Comp: juce::命名空间** | JUCE类型需加`juce::`前缀或`using namespace juce` |
| 2026-05-06 | **VC-Comp: Limiter命名冲突** | 自定义Limiter类名与JUCE冲突，重命名为`VCLimiter` |
| 2026-05-06 | **VC-Comp: getBusBuffer返回值** | 返回AudioBuffer引用，不能取地址，用引用接收 |
| 2026-05-06 | **VC-Comp: ColourGradient构造** | 构造函数需5参数+bool |
| 2026-05-06 | **VC-Comp: XmlDocument::parse** | 需传`String`而非`MemoryBlock` |
| 2026-05-06 | **VC-Comp: Timer继承** | Editor需继承`juce::Timer`并实现timerCallback |
| 2026-05-06 | **GitHub自动化注册失败** | CAPTCHA+浏览器指纹检测，自动化无法绕过，需用户手动注册 |
| 2026-05-06 | **CI中JUCE路径需修改** | CMakeLists.txt写死/opt/JUCE，CI中需sed替换为./JUCE |
| 2026-05-06 | **Linux apt依赖libwebkit2gtk-4.0-dev不存在** | 代码中已禁用webkit/gtk/curl，删除这些包即可，ubuntu-latest已24.04 |
| 2026-05-06 | **macOS sed替换失败** | macOS是BSD sed，语法与GNU sed不同；CMake CACHE变量用-D参数覆盖即可，无需sed |
| 2026-05-06 | **Windows PowerShell正则替换失败** | Set-Content默认UTF-16会破坏源码；同macOS，用-D参数覆盖CACHE变量 |
| 2026-05-06 | **ubuntu-latest升级到24.04** | 部分包名已变更，不要硬编码旧包名 |
| 2026-05-06 | **Mac Xcode generator编译JUCE失败** | JUCE对Xcode generator支持不稳定，改用Ninja generator即可；Mac/Linux统一Ninja更快更稳 |
| 2026-05-06 | **Windows artifact路径匹配失败** | VS generator产物在`xxx_artefacts/Release/`下，glob匹配不可靠；直接上传整个`xxx_artefacts/`目录 |
| 2026-05-06 | **GitHub Actions API调用** | 公开仓库无需认证即可查runs/jobs/steps，但下载日志需admin PAT；详见`知识沉淀/插件开发/GitHub-Actions-VST3编译踩坑记录.md` |
| 2026-05-06 | **JUCE 8.0.0 + macOS Sequoia不兼容** | `CGWindowListCreateImage`在macOS 15.0被废弃，JUCE 8.0.0的juceaide编译失败。升级到8.0.12修复 |
| 2026-05-06 | **Windows runner默认PowerShell** | `2>/dev/null`、`which`、`find`等bash语法在PowerShell中报错，所有run步骤必须加`shell: bash` |
| 2026-05-06 | **CI matrix动态生成** | 不能在matrix中直接用表达式生成plugin列表，需要detect job输出JSON → `fromJson(needs.detect.outputs.matrix).plugin` |
| 2026-05-06 | **push paths过滤与detect逻辑冲突** | on.push.paths只触发包含VC-*的push，但workflow变化也需要触发；需在paths中加`.github/workflows/**`，detect中检测workflow变化则全量构建 |

---

## 与其他技能的协作

### 可调用本技能的场景
- **Auto-RedNote**：小红书音频笔记的混音处理
- **知识沉淀技能**：混音相关知识的沉淀触发
- **topic_tracking**：音频制作话题追踪

### 本技能依赖
- 云电脑环境（音频处理和VST编译验证）
- Python音频库（librosa, scipy, numpy, pydub）
- ffmpeg / SoX
- JUCE框架（VST开发时）
- GitHub Actions CI（跨平台编译，仓库：git@github.com:youbanzhishi/AudioFX.git）
- SSH Key（云电脑 /root/.ssh/id_ed25519，公钥已添加到 GitHub）

### GitHub Actions CI架构（v2）

**三阶段流水线**：
```
┌─ detect ──────────────┐
│  检测VC-*目录变化      │  push: 只构建变化的插件
│  生成matrix JSON       │  手动: 可选all或指定插件
│  workflow变化→全量构建  │  PR: 全量构建
└────────┬───────────────┘
         │ matrix: {plugin: [VC-EQ, VC-Comp]}
         ▼
┌─ build ───────────────┐
│  每插件×每平台独立Job   │  6个job并行（2插件×3平台）
│  各自上传artifact       │  fail-fast: false
└────────┬───────────────┘
         │ (仅手动触发+填写tag时)
         ▼
┌─ release ─────────────┐
│  下载所有artifact      │
│  按平台打包zip         │  VocalChain-macOS.zip
│  发布GitHub Release    │  VocalChain-Windows.zip
└───────────────────────┘  VocalChain-Linux.zip
```

**关键配置**：
- JUCE版本统一在env: JUCE_VERSION: '8.0.12'
- 所有run步骤必须加 `shell: bash`（Windows默认PowerShell不认bash语法）
- push paths: `VC-*/**` + `.github/workflows/**`
- Mac/Linux用Ninja generator，Windows用VS 17 2022

**Actions日志获取三层降级策略**（详见TOOLS.md）：
1. API查询（公开仓库免认证，60次/小时IP限流）
2. 云浏览器（API限流时用agent-browser打开Actions页面）
3. PAT认证（`gh auth login --with-token` → `gh run view --log`，无限流）

---

## 自动更新机制

本技能的踩坑记录和参数优化会随实战不断更新：
1. 每次混音任务完成后，将新的踩坑经验追加到"踩坑记录"章节
2. 参数优化结果实时更新到对应参数表
3. 版本号遵循语义化版本：主版本.次版本.修订号
4. 重大架构变更升主版本，参数调整升次版本，修bug升修订号

---

## 文档化与踩坑经验沉淀规则（强制执行）

### 核心原则

**每一步实操都必须沉淀为文档，每一个坑都必须记录并实时更新。** 这样才能：
- 自己避免重复踩坑
- 分享给他人时别人也能避开
- 知识可积累、可检索、可传承

### 强制执行规则

1. **流程必须文档化**
   - 所有环境搭建、开发部署流程必须写成独立 MD 文档
   - 存放到 `知识沉淀/` 目录，文件名清晰体现内容
   - 示例：`知识沉淀/云电脑JUCE-VST3插件开发环境搭建指南-Ubuntu.md`

2. **踩坑必须记录**
   - 编译报错、环境配置问题、参数调优失败等都属于踩坑
   - 必须记录：问题现象 → 原因分析 → 解决方案
   - 记录位置：技能 SKILL.md 的「已知踩坑记录」章节 + 对应文档的踩坑章节

3. **文档必须实时更新**
   - 问题解决后立即更新文档，不要等"做完再补"
   - 如果之前的解决方案有更好的办法，回头更新旧文档
   - 编译/执行过程中发现新坑，追加到文档而非重写

4. **文档受众 = 其他开发者**
   - 写文档时假设读者是和你一样的开发者，不是自己
   - 必须包含完整的命令、版本号、错误信息原文
   - "一键搭建"命令块必须可用，不能省略步骤

5. **跨平台经验独立成文 + 交叉引用**
   - Mac 开发流程、Ubuntu 云电脑流程各自独立 MD 文档
   - 教程类文档中引用独立文档（而非重复内容）
   - 保持单一信源，避免多处维护同一内容

### 文档化检查清单

每次完成一个实操任务后，必须检查：

| 检查项 | 要求 |
|--------|------|
| 流程文档 | 是否有对应的步骤文档？ |
| 踩坑记录 | 遇到的问题是否已记录到 SKILL.md + 文档？ |
| 实时更新 | 解决新问题后是否更新了已有文档？ |
| 一键可用 | 文档中的命令块能否直接复制执行？ |
| 版本信息 | 文档是否包含工具版本号（GCC/CMake/JUCE等）？ |
| 交叉引用 | 相关文档之间是否互相引用？ |
---

## 五、VCMix 混音宿主

> 详细设计文档：`知识沉淀/插件开发/VCMix-无界面混音宿主设计.md`

### 触发条件
- 用户需要多轨混音项目管理（超过2轨）
- 用户需要 Send/Return 路由、Bus 分组、侧链
- AI Agent 需要自动完成混音流程（零 GUI 操作）

### 三大核心要求

**跨平台 — 全平台可用**
- Windows / macOS / Linux 全覆盖
- Python 核心逻辑天然跨平台
- 音频 I/O 用 soundfile/pydub（跨平台）不用系统 API
- 路径用 pathlib.Path 不用硬编码分隔符
- ffmpeg 调用兼容三平台，CLI 参数和文件编码统一 UTF-8

**资源占用小 + 性能强**
- Python + numpy 向量化运算，避免逐样本循环
- 大文件流式处理（chunk-by-chunk），不全量加载到内存
- 增量渲染：只重算改动的轨道/效果器（SHA-256 缓存指纹）
- 多轨并行：无依赖的轨道用多进程并行渲染
- 目标：10 轨 × 5 分钟项目，渲染时间 < 30 秒

**AI Agent 友好 — 核心差异化（Reaper 没有的）**
- YAML 声明式配置，AI Agent 可直接读写理解整个项目结构
- CLI 零 GUI 操作：`vcmix render/validate/graph` 一条命令完成
- 未来：`vcmix suggest`（AI推荐参数）、`vcmix analyze`（自动诊断）、`vcmix preset`（预设管理）、WebSocket/HTTP API
- 音源分离与逆向分析（Phase 5）：参考曲→逆向分析→自动套用
- 编曲智能模板（Phase 6）：编曲结构/乐器进出/弦乐写法逆向提取→自动套用
- 编曲混音一体化（Phase 7）：SmartTrack+轨道间智能协同+自适应混音
- 设计原则：任何人类通过 GUI 能做的事，AI 通过 YAML+CLI 也要能做到，零 GUI 依赖

### YAML 配置格式概述

```yaml
project:
  name: 九万字
  bpm: 62
  sample_rate: 44100

tracks:
  - name: vocal
    file: 干声.wav
    volume: 0.8
    effects:
      - VC-DeEsser: {threshold: -40, reduction: -6}
      - VC-Gain: {gain: 6}
      - VC-EQ: {}
      - VC-Comp: {}
    sends:
      - {bus: reverb, level: 0.10}
      - {bus: delay, level: 0.05}

buses:
  - name: reverb
    effects:
      - VC-Reverb: {room: 30, decay: 35, mix: 100, wetlpf: 5000}
  - name: delay
    effects:
      - VC-Delay: {time: 1/4, feedback: 12, mix: 100}  # BPM同步

master:
  effects:
    - VC-Comp: {ratio: 2, threshold: -6}
    - VC-Limiter: {ceiling: -1.0}
  output: 混音输出.mp3
```

### CLI 接口

```bash
vcmix render project.yaml           # 渲染混音项目
vcmix render project.yaml --force   # 强制全量渲染
vcmix render project.yaml -t 20     # 20秒截断测试
vcmix validate project.yaml         # 验证YAML配置
vcmix graph project.yaml            # 输出信号路由图
vcmix compare A.yaml B.yaml         # A/B对比渲染
vcmix cache project.yaml list       # 缓存管理
```

### 里程碑进度（M0~M14）

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M0 | 设计文档完成 | ✅ |
| M1 | YAML 解析器 + 参数校验 | 📋 |
| M2 | 信号路由图构建 + 拓扑排序 | 📋 |
| M3 | 插件适配器框架 | 📋 |
| M4 | 基本轨道 + Insert + Master 输出 | 📋 |
| M5 | CLI render/validate 命令 | 📋 |
| M6 | 20秒截断测试 | 📋 |
| M7 | Send/Return 总线 | 📋 |
| M8 | Bus 效果链 + 并行渲染 | 📋 |
| M9 | BPM 同步换算 | 📋 |
| M10 | 侧链路由 | 📋 |
| M11 | 增量渲染 + 缓存管理 | 📋 |
| M12 | 参数自动化（envelope） | 📋 |
| M13 | A/B 对比 + MP3 输出 | 📋 |
| M14 | vcmix suggest/analyze 初版 | 📋 |
| M15 | 音源分离与逆向分析（Demucs拆分+混音/编曲手法逆向→VCMix配置） | 📋 |
| M16 | 编曲智能模板（编曲结构/乐器进出/弦乐写法逆向提取→自动套用） | 📋 |
| M17 | 编曲混音一体化（SmartTrack+轨道间智能协同+自适应混音） | 📋 |
| M18 | 完整开源DAW（GUI+VST3 Hosting+MIDI+编曲+混音一体化+AI扒带+AI Agent API） | 📋 |

---

## 六、开源 DAW 路线图

### 阶段路径

```
VC 插件（当前）          VCMix（进行中）           完整 DAW（远期）
┌──────────┐        ┌──────────────┐        ┌──────────────┐
│ 10个VC插件 │  ───> │ YAML驱动宿主  │  ───> │  开源 DAW    │
│ CLI+VST3  │        │ AI Agent友好  │        │  GUI + 实时  │
│ 独立可用   │        │ 离线渲染      │        │  录音+MIDI   │
└──────────┘        └──────────────┘        └──────────────┘
```

### 总方针

**Reaper 有的我们要有，Reaper 没有的我们也要有**

- Reaper 有的：无限轨道、插件 hosting、MIDI、自动化 envelope、侧链、发送总线、脚本扩展
- Reaper 没有的：AI 原生设计、YAML 声明式配置、零 GUI 可操作、自动音频分析

### AI Agent 差异化

开源 DAW 的核心差异化竞争力是 **AI Agent 原生设计**：

1. **YAML 声明式**：项目文件即配置，AI 可直接读写理解
2. **CLI 零 GUI**：所有操作可通过命令行完成，无需图形界面
3. **自动分析**：AI 可自动诊断音频问题并生成修复方案
4. **参数推荐**：AI 根据音频特征推荐效果器参数
5. **API 驱动**：WebSocket/HTTP API 实现实时 Agent 通信
6. **AI扒带能力**：参考曲→逆向分析→自动套用（M15）
7. **编曲分析能力**：曲式/乐器进出/弦乐写法逆向提取→自动套用（M16）
8. **编曲混音一体化**：SmartTrack+轨道间智能协同+自适应混音（M17）

**设计原则：任何人类通过 GUI 能做的事，AI 通过 YAML+CLI 也要能做到，零 GUI 依赖**
